{
  "name": "rakshak-ai-server",
  "version": "1.0.0",
  "description": "Python server wrapper",
  "scripts": {
    "start": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
  }
}
